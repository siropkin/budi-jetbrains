pluginGroup = com.github.siropkin.budijetbrains
pluginRepositoryUrl = https://github.com/siropkin/budi-jetbrains
pluginVersion = 0.1.6

# IntelliJ Platform compatibility window.
# 242 = 2024.2 — floor keeps K2-era Kotlin and modern coroutines available
# while staying broad enough for most JetBrains IDE installs in 2026.
# 261.* = 2026.1.x — widened so users on current EAP/2026.1 builds can
# install. The plugin is widget-only (StatusBarWidget + Configurable +
# NotificationGroupManager — all stable APIs since 2024.2), so the
# forward-compat risk is small. The `recommended()` verifier may not yet
# carry IDEs past 252; bump in lockstep as new IDE majors land in the
# verifier matrix.
pluginSinceBuild = 242
pluginUntilBuild = 261.*

# IntelliJ Platform Gradle Plugin — target IDE for the build.
# IC = IntelliJ IDEA Community. The widget-only plugin we ship has no
# IC-vs-IU divergent code paths, so building against IC keeps the
# verifier matrix simpler and the test runIde faster.
platformType = IC
platformVersion = 2024.2

# Java toolchain. The Gradle version itself is pinned by
# gradle/wrapper/gradle-wrapper.properties.
javaVersion = 21

# Opt-out flag for bundling Kotlin standard library -> https://jb.gg/intellij-platform-kotlin-stdlib
kotlin.stdlib.default.dependency = false

# Enable Gradle Configuration Cache -> https://docs.gradle.org/current/userguide/configuration_cache.html
org.gradle.configuration-cache = true

# Enable Gradle Build Cache -> https://docs.gradle.org/current/userguide/build_cache.html
org.gradle.caching = true
